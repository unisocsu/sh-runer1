<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.shellrunner">

    <!-- הרשאות גישה לאחסון וניהול קבצים (קריטי עבור סייר קבצים) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    
    <!-- הרשאה מיוחדת לאנדרואיד 11 ומעלה לניהול מלא של כל הקבצים במכשיר -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />

    <!-- הרשאות רשת (במקרה שהסקריפטים צריכים להוריד קבצים או לתקשר עם שרתים) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- הרשאה להרצת שירות ברקע (עבור סקריפטים שרצים זמן ממושך) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Shell Runner"
        android:theme="@android:style/Theme.DeviceDefault">
        
        <!-- הגדרה המאפשרת גישה לאחסון הישן באנדרואיד 10 -->
        <application
            android:requestLegacyExternalStorage="true">
        </application>

        <!-- ה-Activity הראשי של האפליקציה הכולל תמיכה בקלט מקלדת וחיצים -->
        <activity 
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|keyboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
    </application>
</manifest>
